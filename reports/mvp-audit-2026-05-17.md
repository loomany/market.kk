# MVP Audit Report — Kaspi AI Product Photo Studio

**Date:** 2026-05-17  
**Scope:** Stages 1–7 (MVP only, no Stage 8)  
**Real Fal calls:** Not executed (mock mode only)

---

## Implemented

| Area | Status | Notes |
|------|--------|-------|
| Landing `/` | Done | Hero, How it works, Use cases |
| Studio `/studio` | Done | Mode selector, settings, results |
| Clothing Try-On | Done | `POST /api/ai/tryon`, FASHN v1.6 |
| AI model generation | Done | `POST /api/ai/generate-model`, nano-banana-2 |
| Local upload | Done | Multipart → Fal Storage (real); mock validates only |
| Product Shot | Done | `POST /api/ai/product-shot`, Bria |
| Background remove | Done | Per-card + bg-only mode |
| QC checklist | Done | 8 items, Accept/Reject/Download gating |
| Regenerate | Done | Try-on + product-shot; seed for try-on/model |
| Mock mode | Done | `AI_MOCK_MODE !== "0"` |

## Studio modes

1. **clothing-tryon** — product + model → try-on → QC → optional bg remove  
2. **product-shot** — product + scene presets → product shot → QC  
3. **background-remove-only** — image URL → single result (no regenerate)

## Verification performed

| Check | Result |
|-------|--------|
| `npm run build` | Pass |
| `npm run lint` | Pass |
| `FAL_KEY` grep | Server-only (`lib/ai/falClient.ts`, API routes, `.env.example`) |
| No `NEXT_PUBLIC_FAL_KEY` | Confirmed |
| No `FAL_KEY` in `components/` | Confirmed |
| `.env.local` gitignored | `git check-ignore .env.local` → yes |
| `.env.example` committable | `!.env.example` in `.gitignore` |
| `node_modules`, `.next` ignored | Confirmed |

### Commands run (audit session)

```bash
npm run build
npm run lint
git status --short
git check-ignore -v .env.local .env.example node_modules .next
```

### Build result

- Next.js 16.2.6 — compiled successfully  
- Routes: `/`, `/studio`, 4 API routes  

### Lint result

- ESLint — no errors  

## Security: FAL_KEY

| Location | Allowed |
|----------|---------|
| `lib/ai/falClient.ts` | Yes (server-only import) |
| `app/api/ai/*/route.ts` | Yes |
| `.env.example`, README | Yes (placeholder/docs) |
| Client components | **No** — verified |

`fal.config({ credentials })` only in `falClient.ts` with `import "server-only"`.

## Not done (by design)

- Supabase / Postgres / Storage  
- Auth (login, sessions)  
- Payments / credits  
- Generation history DB  
- Batch queue / webhooks  
- Server-side image resize export  
- Real paid Fal API calls during MVP build  
- `git push` (per audit instructions)  

## Risks

| Risk | Severity | Mitigation |
|------|----------|------------|
| Mock images ≠ real Fal quality | Medium | Controlled real test with approve + small `numSamples` |
| Cross-origin download may open tab instead of save | Low | Documented; future proxy optional |
| BG-only mode no file upload | Low | Stage 8 or follow-up |
| `npm audit` moderate advisories (Next deps) | Low | Track upstream updates |
| No automated E2E tests | Medium | Manual QA checklist in README |

## Readiness for controlled real Fal test

**Ready** when:

1. `AI_MOCK_MODE=0` and valid `FAL_KEY` in `.env.local`  
2. Owner approve for paid calls  
3. Start with: 1 sample try-on, 1 product-shot, verify URLs from Fal CDN  
4. Monitor Fal dashboard for spend  

## Conclusion

MVP Stages 1–7 are **complete** for mock-mode development and demo. Safe to commit and push to `loomany/market.kk` after review. Real Fal validation is a separate gated step.

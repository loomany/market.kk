# SEO example assets (owner-only)

Place **owned** or **licensed** before/after images here before enabling examples index.

- Use paths like `/examples/product-photos/case-001-before.webp`
- Register each file in `data/seo/exampleCases.ts` with `assetStatus: "owned" | "licensed"` and `isRealOwnedAsset: true`
- Do **not** copy `public/demo/*.svg` — those are Studio mocks only
- Owner must approve index in a later stage (12.1+)
